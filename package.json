{
  "name": "flowpilot",
  "version": "0.3.0",
  "license": "MIT",
  "bin": {
    "flow": "./dist/flow.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/main.ts",
    "test": "vitest",
    "setup": "npm run build && npm link"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
